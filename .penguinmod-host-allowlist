# Files exempt from the "no upstream hosts" CI guard.
# Only intentional upstream references remain: rewrite tooling (pull.js), the
# canonical host MAP (pm-config.js), the success-criterion text (.env*), and docs.
# All runtime/user-facing hosts were repointed to patternyard.dev on 2026-06-21.

**/*.md
**/*.mdx
.env
.env.example
src/lib/pm-config.js
src/addons/pull.js
