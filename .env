# wycats deployment config. NOTE: PM_API_ROOT endpoint parity with upstream is
# pending the Stage 6 backend-independence audit before relying on it in prod.
PM_API_ROOT=https://penguinmod-backend.vercel.app
